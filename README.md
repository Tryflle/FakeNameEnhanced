# FakeNameEnhanced
1.8.9 forge rewritten and better version of fakename that works with patcher optimized font renderer
# Usage
`/fakename <name>` - set name <br >
`/fakename` - reset name <br >
`/fakenamecolor` - shows all enumchatformatting options you can use